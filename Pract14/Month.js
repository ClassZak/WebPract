class Month{
    constructor(name, imageSource, number, seasonNumber){
        this.name = name || '';
        this.imageSource = imageSource || '';
        this.number = number || 1;
        this.seasonNumber = seasonNumber || 1;
    }
}

//export default Month;